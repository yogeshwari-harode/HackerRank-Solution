#include<bits/stdc++.h>
using namespace std;

void Array_rotation(int n,int k,vector<int>&arr)
{
    for(int i=k ; i<n ; i++)
    cout<<arr[i]<<" ";
    for(int i=0 ; i<k ; i++)
    cout<<arr[i]<<" ";
}

int main()
{
    int n,k;
    cin>>n>>k;
    vector<int>arr(n);
    for(int i=0 ; i<n ; i++)
    cin>>arr[i];
    
    Array_rotation(n,k,arr);
    return 0;
}
